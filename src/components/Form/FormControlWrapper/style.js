import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  FormControlWrapper: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    width: "100%",
    marginBottom: "24px",
  },
});
